{
  "name": "fairground",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "FAIRGROUND — the honest carnival. Paint your prizes, the wheel stays fair. Chain Jam Vol. 1 entry.",
  "scripts": {
    "dev": "vite --port 3120 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 3121 --strictPort",
    "typecheck": "tsc --noEmit",
    "verify:rtp": "node scripts/verify-rtp.mjs",
    "verify:missions": "node scripts/verify-missions.mjs"
  },
  "dependencies": {
    "penpal": "^7.0.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
